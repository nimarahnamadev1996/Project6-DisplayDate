# Project6-DisplayDate
interactive React Counter application that allows users to increment and decrement a value while customizing the step size. Users can define their own increment/decrement range, making the counter more flexible than a traditional counter app. The project focuses on React state management, event handling, controlled inputs, and dynamic UI updates.
